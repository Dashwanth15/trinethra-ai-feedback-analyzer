Your previous response failed: {{ previous_error }}

Previous output:
{{ previous_raw_output }}

Fix the JSON and remove any fabricated content. Return ONLY raw JSON.

Rules:
- NO markdown blocks. NO prose. Raw JSON only.
- Every quote must be a VERBATIM substring of the transcript.
- Allowed KPIs only: Employee Engagement, Performance, Retention, Communication, Leadership, Development, Well-being, Collaboration.
- If uncertain, use lower confidence and empty arrays. Never invent data.
